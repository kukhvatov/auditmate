services:
  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      - "8000:8000"

  frontend:
    build: ./front
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
